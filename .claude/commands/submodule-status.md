---
description: Show git status of all three submodules
---

# Submodule Status

Run the submodule status script to see the current state of all three submodules:

```bash
.claude/scripts/submodule-status.sh
```

This shows: branch, dirty/clean state, ahead/behind remote, and last commit for each of ios/, district/, and graph/.
